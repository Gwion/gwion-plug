
SinOsc s => dac;
//adc => 
second => now;
adc => ProjectM pm => dac;
//s => pm.chan(0);
//s => pm.chan(1);
//s => dac;
adc => dac;

second => now;
<<<adc.last()>>>;

while(true) {
	<<<adc.last()>>>;
	<<<adc.chan(0).last()>>>;
	<<<adc.chan(1).last()>>>;
//	<<<s.last(), pm.last()>>>;
	.5::second => now;

}

