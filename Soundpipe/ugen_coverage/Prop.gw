#import Soundpipe
#! coverage for 'Prop'. (generated by util/coverage.sh)

var Prop a ~> dac;
samp => now;
a ~< dac;

#! testing 'init'
{
	"+ + 3(+ - +) 2(- 3(+ + +)) 3(+ +) + 4(+ +)" :=> var string	str;

	a.init(str);a.init(str);
}

#! testing 'bpm'
{

	a.bpm();
}

#! testing 'bpm'
{
	var float	bpm;

	a.bpm(bpm);
}
samp => now;
a ~> dac;
second => now;
