#! coverage for 'Prop'._ (generated by util/coverage._sh)

Prop a => dac;
samp => now;
a =< dac;

#! testing 'init'
{
	"+ + 3(+ - +) 2(- 3(+ + +)) 3(+ +) + 4(+ +)" => string	str;

	a._init(str);a._init(str);
}

#! testing 'bpm'
{

	a._bpm();
}

#! testing 'bpm'
{
	float	bpm;

	a._bpm(bpm);
}
samp => now;
a=> dac;
second => now;
